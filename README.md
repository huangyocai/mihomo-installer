# mihomo-installer

One-shot installer for mihomo on RHEL/OpenCloudOS (x86_64), with systemd + optional metacubexd UI.

## Usage
sudo SUB_URL="YOUR_SUB_URL" SECRET="YOUR_SECRET" INSTALL_UI=1 ./install_mihomo.sh
